@model IEnumerable<RealEstateApp.Models.Message>

@{
    ViewData["Title"] = "Входящи съобщения💌";
}

<h2>📥 Входящи съобщения</h2>

@if (!Model.Any())
{
    <p>Нямате входящи съобщения.</p>
}
else
{
    <table class="table table-dark table-hover text-center">
        <thead>
            <tr>
                <th>От</th>
                <th>Тема</th>
                <th>Съдържание</th>
                <th>Изпратено на</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var msg in Model)
            {
                <tr>
                    <td>@msg.Sender.Email</td>
                    <td>@msg.Subject</td>
                    <td>@msg.Content</td>
                    <td>@msg.SentAt.ToLocalTime()</td>
                </tr>
            }
        </tbody>
    </table>
}
