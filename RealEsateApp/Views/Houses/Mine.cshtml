@using RealEstateApp.Models
@model IEnumerable<RealEstateApp.Models.House>
@{
    ViewData["Title"] = "Моите имоти";
}

<h2 class="text-center mb-4">Моите имоти</h2>

@if (!Model.Any())
{
    <p class="text-center">Нямате добавени имоти.</p>
}
else
{
    <div class="row row-cols-1 row-cols-md-3 g-4">
        @foreach (var house in Model)
        {
            <div class="col">
                <div class="card h-100 shadow-sm">
                    @if (!string.IsNullOrWhiteSpace(house.ImageUrl))
                    {
                        <img src="@house.ImageUrl" class="card-img-top" alt="Снимка на имота" style="height: 200px; object-fit: cover;" />
                    }
                    else
                    {
                        <div class="bg-secondary text-white d-flex align-items-center justify-content-center" style="height: 200px;">
                            <span>Няма снимка</span>
                        </div>
                    }

                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@house.Title</h5>
                        <p class="card-text">@house.Description?.Substring(0, Math.Min(100, house.Description.Length))...</p>
                        <p class="card-text"><strong>Цена:</strong> @house.Price лв</p>
                        <p class="card-text"><strong>Тип:</strong> @(house.PropertyType == PropertyType.ForSale ? "За продажба" : "Под наем")</p>
                        <p class="card-text"><strong>Категория:</strong> @house.Category</p>

                        <div class="mt-auto">
                            <a asp-action="Details" asp-route-id="@house.Id" class="btn btn-primary btn-sm me-2">👁 Детайли</a>
                            <a asp-action="Edit" asp-route-id="@house.Id" class="btn btn-warning btn-sm me-2">✏️ Редактирай</a>
                            <a asp-action="DeleteMine" asp-route-id="@house.Id" class="btn btn-danger btn-sm">🗑️ Изтрий</a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}
